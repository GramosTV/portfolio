// This is the new root layout at src/app/layout.tsx
// It is responsible for rendering the [locale] layout.

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
